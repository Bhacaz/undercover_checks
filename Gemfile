source 'https://rubygems.org'

git_source(:github) { |repo_name| "https://github.com/#{repo_name}" }

# Specify your gem's dependencies in delta-simplecov-checks.gemspec
gemspec

gem 'jwt', require: false
gem 'undercover'

group :test do
  gem 'rake', '~> 12.0'
  gem 'rspec', '~> 3.0'
end
